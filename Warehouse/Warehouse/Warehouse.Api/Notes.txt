Add swagger configuration
Add jwt
Sort out startup into configuration files
Add cors + request redirection
Add authentication
Add mini etigrant
Probably frontend

ToDo:
Poprawić kontrolery i serwisy (REST, commandy/query, dto)
Poprawić encję - dodać wszystkie propertiesy encji do konstruktorów
